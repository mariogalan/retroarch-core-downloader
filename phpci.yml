build_settings:
    verbose: true
    prefer_symlink: false

setup:
  composer:
    action: "install"

test:
    php_unit:
        directory: "vendor/bin/phpunit/"
        args: "--configuration 'phpunit.xml'"

complete: