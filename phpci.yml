build_settings:
    verbose: true
    prefer_symlink: false

setup:

test:
    php_unit:
        directory: "vendor/bin/phpunit/"
#        args: "--bootstrap 'test/phpunit/bootstrapunit.xml'"

complete: